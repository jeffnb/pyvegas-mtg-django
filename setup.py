#!/usr/bin/env python
"""Setup script for the Magic the Gathering Django ORM demo project.

This is a minimal setup script that defers to pyproject.toml for configuration.
It's included for compatibility with traditional Python packaging tools.

For installation, it's recommended to use uv:
    uv pip install -e .
"""

from setuptools import setup

if __name__ == "__main__":
    setup()